# TODO

- Self-host or proxy third-party Activity page assets:
  - Serve `Chart.js` from this site instead of `cdn.jsdelivr.net`.
  - Replace the live GitHub contribution image with a locally hosted snapshot or a same-origin proxy endpoint.
  - Revisit CSP/security headers after those third-party browser requests are removed.
